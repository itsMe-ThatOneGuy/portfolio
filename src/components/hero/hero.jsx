const Hero = () => {
    return (
        <section className="border-2 border-red-900 h-screen">
            <div className="flex flex-col justify-end items-center min-h-screen">
                <button className="text-center mb-44 p-3 px-7 text-lg tracking-widest font-bold border-2 border-black rounded-full">
                    Resume
                </button>
            </div>
        </section>
    );
};

export default Hero;
